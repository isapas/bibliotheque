{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>La liste des catégories</h1>

    <table class="table mb-5 d-flex justify-content-center text-left flex-wrap">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ category.category }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex flex-row bd-highlight mb-3 justify-content-center">
        <a class="btn btn-primary ml-5 mr-5" href="{{ path('category_edit', {'id': category.id}) }}">Modifier</a>
        {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
